using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FloatingTeleporterScript : MonoBehaviour
{
    [SerializeField]
    List<GameObject> crystals;
    // Start is called before the first frame update
    void Start()
    {
        //foreach (GameObject gameObject in crystals)
        //{
        //    LeanTween.move
        //}
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
